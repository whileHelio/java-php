<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helio.projectoandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <activity
            android:name=".GameActivity"
            android:label="@string/nivel_1" />
        <activity
            android:name=".Nivel2Activity"
            android:label="@string/nivel_2" />
        <activity
            android:name=".loadsActivity"
            android:label="@string/JogosGrava" />
        <activity android:name=".settingsActivity"
            android:label="@string/defini"
            ></activity>
    </application>

</manifest>
